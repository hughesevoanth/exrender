---
title: "Test Report Render"
author: "David Hughes"
date: "`r Sys.Date()`"
papersize: a4
fontsize: 12pt
mainfont: Arial
output: 
  pdf_document:
    latex_engine: pdflatex
    toc: true
    fig_caption: true
    keep_md: true
    number_sections: true
params: 
 Rdatafile: NA
 out_dir: out_dir
---


```{r setup, include=FALSE}
suppressPackageStartupMessages(library(knitr))
suppressPackageStartupMessages(library(tidyverse))

knitr::opts_chunk$set(warning=FALSE, echo=TRUE, message=FALSE, 
               results='asis', fig.width=6, fig.height=6 )
```

```{r}
f = system.file("rmarkdown", package="exrender")
pic = file.path( f, "skeleton/daisy.png")
knitr::include_graphics(pic)
```


```{r}
# load(params$Rdatafile)
# out_dir = params$out_dir
```


## Test R package report

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
message( "Summary of cars" )
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
message( "Plot of pressure" )
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


```{r}
message("Plot of iris PCA")
# my_plot
```

```{r}
message("access the internal data ng_anno")
# head(ng_anno)
```



